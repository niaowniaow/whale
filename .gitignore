# Rust
/target/

# Editor/IDE
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Swap/backup files
*.swp
*.*~


venv/
scratch/

# Training outputs and datasets
/checkpoints/
/data/
**/nnue.bin
# Best-so-far big checkpoint written by `train::copy_trained_weights`
# (BIG_KEEPER_PATH) — never commit a ~100 MB training artifact.
resources/sfnn16-big-checkpoint.bin
*.epd
*.binpack
*.binpack.meta
*.rtbw
*.rtbz
checksum.md5


# cargo-mutants
*mutants.out*

# Binaries and networks
*.exe
*.nnue
!models/*.nnue
*.pgn
/nnue-pytorch/
/tools/nnue-pytorch/
/v16/

# Syzygy tablebases (large, download separately)
/tables/*
*.rtbw
*.rtbz
# ...except the tiny 3-piece tables: unit tests (incl. CI) probe them.
!/tables/KQvK.rtbw
!/tables/KQvK.rtbz
!/tables/KRvK.rtbw
!/tables/KRvK.rtbz
!/tables/KBNvK.rtbw
!/tables/KBNvK.rtbz
/Stockfish/
/Reckless/
/lc0/

# Local build artifacts / probe output at repo root (never commit binaries)
/whale
/whale-*
/fens_out.txt
/depth_out.txt
/kiwi_out.txt
/probe_*.py
/scratch_test.py
/in.txt

# Test fixtures: tiny EPD suites required by tests/epd_suite.rs (keep tracked)
!/data/
!/data/benchmarks/
!/data/benchmarks/*.epd
!/resources/openings.epd
